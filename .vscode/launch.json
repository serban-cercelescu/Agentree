{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Agentree extension",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}/vscode-ext"],
      "preLaunchTask": "build vscode extension"
    }
  ]
}
